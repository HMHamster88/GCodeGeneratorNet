<Window x:Class="GCodeGeneratorNet.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MainWindow" Height="350" Width="525">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Menu Grid.ColumnSpan="2">
            <MenuItem Header="File">
                <MenuItem Header="Compile" Click="MenuItem_Click_1"></MenuItem>
            </MenuItem>
            <MenuItem Header="View">
                <MenuItem Header="Show 3D" Click="MenuItem_Click"/>
            </MenuItem>
        </Menu>
        <TextBox TextWrapping="Wrap" AcceptsReturn="True" Grid.Row="1" Text="{Binding TextEditManager.Text}"/>
    </Grid>
</Window>
